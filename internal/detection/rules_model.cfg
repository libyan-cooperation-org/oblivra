SPECIFICATION Spec
\* Add arbitrary constraint to keep model finite
INVARIANT NoSpuriousAlerts
INVARIANT WindowStateInvariant

CONSTANTS
    Steps = << "fail1", "fail2", "success" >>
    WindowSize = 10
    MaxEvents = 5
