// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as database$0 from "../database/models.js";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * @param {any} fn
 * @returns {$CancellablePromise<void>}
 */
export function AccessMasterKey(fn) {
    return $Call.ByID(2580669392, fn);
}

/**
 * @param {string} label
 * @param {string} credType
 * @param {string} rawData
 * @returns {$CancellablePromise<string>}
 */
export function AddCredential(label, credType, rawData) {
    return $Call.ByID(1115050245, label, credType, rawData);
}

/**
 * @param {string} data
 * @returns {$CancellablePromise<string>}
 */
export function Decrypt(data) {
    return $Call.ByID(2712060538, data).then(/** @type {($result: any) => any} */(($result) => {
        return $Create.ByteSlice($result);
    }));
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<void>}
 */
export function DeleteCredential(id) {
    return $Call.ByID(806130487, id);
}

/**
 * Dependencies returns service dependencies
 * @returns {$CancellablePromise<string[]>}
 */
export function Dependencies() {
    return $Call.ByID(25351616).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * @param {string} data
 * @returns {$CancellablePromise<string>}
 */
export function Encrypt(data) {
    return $Call.ByID(3507882818, data).then(/** @type {($result: any) => any} */(($result) => {
        return $Create.ByteSlice($result);
    }));
}

/**
 * @param {string} label
 * @returns {$CancellablePromise<string>}
 */
export function GenerateEd25519Key(label) {
    return $Call.ByID(168514230, label);
}

/**
 * GeneratePassword creates a cryptographically secure random password.
 * Uses rejection sampling via math/big to eliminate modulo bias.
 * @param {number} length
 * @param {boolean} includeSymbols
 * @returns {$CancellablePromise<string>}
 */
export function GeneratePassword(length, includeSymbols) {
    return $Call.ByID(2008864351, length, includeSymbols);
}

/**
 * @param {string} id
 * @returns {$CancellablePromise<string>}
 */
export function GetDecryptedCredential(id) {
    return $Call.ByID(2456614780, id);
}

/**
 * GetPassword retrieves a password by ID and decrypts it into a volatile, mutable byte slice.
 * @param {string} id
 * @returns {$CancellablePromise<string>}
 */
export function GetPassword(id) {
    return $Call.ByID(325195486, id).then(/** @type {($result: any) => any} */(($result) => {
        return $Create.ByteSlice($result);
    }));
}

/**
 * GetPrivateKey retrieves a private key by ID and decrypts it
 * @param {string} id
 * @returns {$CancellablePromise<[string, string]>}
 */
export function GetPrivateKey(id) {
    return $Call.ByID(4253187033, id).then(/** @type {($result: any) => any} */(($result) => {
        $result[0] = $Create.ByteSlice($result[0]);
        return $result;
    }));
}

/**
 * @returns {$CancellablePromise<string>}
 */
export function GetYubiKeySerial() {
    return $Call.ByID(1623281103);
}

/**
 * HasKeychainEntry returns true if the OS keychain has a stored vault credential,
 * meaning a headless auto-unlock attempt is likely to succeed.
 * Returns false on any error — never panics.
 * @returns {$CancellablePromise<boolean>}
 */
export function HasKeychainEntry() {
    return $Call.ByID(2506776209);
}

/**
 * @returns {$CancellablePromise<boolean>}
 */
export function IsSetup() {
    return $Call.ByID(1816063078);
}

/**
 * @returns {$CancellablePromise<boolean>}
 */
export function IsTPMBound() {
    return $Call.ByID(4009335660);
}

/**
 * @returns {$CancellablePromise<boolean>}
 */
export function IsUnlocked() {
    return $Call.ByID(1019715660);
}

/**
 * @param {string} typeFilter
 * @returns {$CancellablePromise<database$0.Credential[]>}
 */
export function ListCredentials(typeFilter) {
    return $Call.ByID(90556741, typeFilter).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType2($result);
    }));
}

/**
 * @returns {$CancellablePromise<void>}
 */
export function Lock() {
    return $Call.ByID(3392242338);
}

/**
 * @returns {$CancellablePromise<string>}
 */
export function Name() {
    return $Call.ByID(1326622278);
}

/**
 * @returns {$CancellablePromise<void>}
 */
export function NuclearDestruction() {
    return $Call.ByID(3551489167);
}

/**
 * PasswordHealthAudit scans all credentials and returns a health report
 * @returns {$CancellablePromise<$models.CredentialHealth[]>}
 */
export function PasswordHealthAudit() {
    return $Call.ByID(3054968969).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType4($result);
    }));
}

/**
 * ResetVault wipes the vault file so the user can set up a fresh vault.
 * ALL ENCRYPTED DATA IS PERMANENTLY LOST. This is an emergency escape hatch.
 * @returns {$CancellablePromise<void>}
 */
export function ResetVault() {
    return $Call.ByID(816966436);
}

/**
 * SetContext updates the Wails runtime context after DomReady.
 * Called by app.Startup so EventsEmit uses the correct context.
 * @returns {$CancellablePromise<void>}
 */
export function SetContext() {
    return $Call.ByID(1900248426);
}

/**
 * @param {string} password
 * @param {string} yubiKeySerial
 * @returns {$CancellablePromise<void>}
 */
export function Setup(password, yubiKeySerial) {
    return $Call.ByID(4231588886, password, yubiKeySerial);
}

/**
 * SetupVault is a convenience alias used by the Wails frontend binding.
 * @param {string} password
 * @returns {$CancellablePromise<void>}
 */
export function SetupVault(password) {
    return $Call.ByID(2755375050, password);
}

/**
 * SetupVaultWithTPM sets up the vault with TPM 2.0 PCR binding.
 * @param {string} password
 * @param {number} pcr
 * @returns {$CancellablePromise<void>}
 */
export function SetupVaultWithTPM(password, pcr) {
    return $Call.ByID(1733086845, password, pcr);
}

/**
 * SetupVaultWithYubiKey sets up the vault with a YubiKey serial.
 * @param {string} password
 * @param {string} serial
 * @returns {$CancellablePromise<void>}
 */
export function SetupVaultWithYubiKey(password, serial) {
    return $Call.ByID(4269246636, password, serial);
}

/**
 * SetupWithTPM satisfies the vault.Provider interface.
 * @param {string} password
 * @param {string} yubiKeySerial
 * @param {number} pcr
 * @returns {$CancellablePromise<void>}
 */
export function SetupWithTPM(password, yubiKeySerial, pcr) {
    return $Call.ByID(1839744017, password, yubiKeySerial, pcr);
}

/**
 * @returns {$CancellablePromise<void>}
 */
export function Start() {
    return $Call.ByID(4276600511);
}

/**
 * @returns {$CancellablePromise<void>}
 */
export function Stop() {
    return $Call.ByID(2792933445);
}

/**
 * @returns {$CancellablePromise<void>}
 */
export function TryAutoUnlock() {
    return $Call.ByID(394421137);
}

/**
 * @param {string} password
 * @param {string} hardwareKey
 * @param {boolean} rememberMe
 * @returns {$CancellablePromise<void>}
 */
export function Unlock(password, hardwareKey, rememberMe) {
    return $Call.ByID(2013471669, password, hardwareKey, rememberMe);
}

/**
 * UnlockWithHardware unlocks the vault using a password combined with a hardware key.
 * This is the Wails-facing method for hardware key unlock.
 * @param {string} password
 * @param {string} hardwareKey
 * @param {boolean} remember
 * @returns {$CancellablePromise<void>}
 */
export function UnlockWithHardware(password, hardwareKey, remember) {
    return $Call.ByID(155036549, password, hardwareKey, remember);
}

/**
 * @returns {$CancellablePromise<void>}
 */
export function UnlockWithKeychain() {
    return $Call.ByID(68661785);
}

/**
 * @param {string} password
 * @param {boolean} remember
 * @returns {$CancellablePromise<void>}
 */
export function UnlockWithPassword(password, remember) {
    return $Call.ByID(654028996, password, remember);
}

/**
 * UpdateCredential modifies an existing credential's label, type, or data
 * @param {string} id
 * @param {string} label
 * @param {string} credType
 * @param {string} rawData
 * @returns {$CancellablePromise<void>}
 */
export function UpdateCredential(id, label, credType, rawData) {
    return $Call.ByID(22623365, id, label, credType, rawData);
}

// Private type creation functions
const $$createType0 = $Create.Array($Create.Any);
const $$createType1 = database$0.Credential.createFrom;
const $$createType2 = $Create.Array($$createType1);
const $$createType3 = $models.CredentialHealth.createFrom;
const $$createType4 = $Create.Array($$createType3);
