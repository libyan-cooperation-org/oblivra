// @ts-check
// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

/**
 * LocalService manages local terminal sessions.
 * @module
 */

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Call as $Call, CancellablePromise as $CancellablePromise, Create as $Create } from "@wailsio/runtime";

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import * as $models from "./models.js";

/**
 * CloseSession kills the session.
 * @param {string} sessionID
 * @returns {$CancellablePromise<void>}
 */
export function CloseSession(sessionID) {
    return $Call.ByID(47219102, sessionID);
}

/**
 * Dependencies returns service dependencies
 * @returns {$CancellablePromise<string[]>}
 */
export function Dependencies() {
    return $Call.ByID(1696022347).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType0($result);
    }));
}

/**
 * @param {string} ctxID
 * @param {string} path
 * @returns {$CancellablePromise<$models.FileInfo[]>}
 */
export function ListDirectory(ctxID, path) {
    return $Call.ByID(617829433, ctxID, path).then(/** @type {($result: any) => any} */(($result) => {
        return $$createType2($result);
    }));
}

/**
 * @param {string} ctxID
 * @param {string} path
 * @returns {$CancellablePromise<void>}
 */
export function Mkdir(ctxID, path) {
    return $Call.ByID(2211133197, ctxID, path);
}

/**
 * @returns {$CancellablePromise<string>}
 */
export function Name() {
    return $Call.ByID(2504073773);
}

/**
 * @param {string} ctxID
 * @param {string} path
 * @returns {$CancellablePromise<string>}
 */
export function ReadFile(ctxID, path) {
    return $Call.ByID(1102348416, ctxID, path);
}

/**
 * @param {string} ctxID
 * @param {string} path
 * @returns {$CancellablePromise<void>}
 */
export function Remove(ctxID, path) {
    return $Call.ByID(3436176426, ctxID, path);
}

/**
 * @param {string} ctxID
 * @param {string} old
 * @param {string} $new
 * @returns {$CancellablePromise<void>}
 */
export function Rename(ctxID, old, $new) {
    return $Call.ByID(494847112, ctxID, old, $new);
}

/**
 * Resize changes the terminal dimensions. On unix with PTY this sends a real
 * SIGWINCH signal. On Windows (pipe mode) this is a no-op.
 * @param {string} sessionID
 * @param {number} cols
 * @param {number} rows
 * @returns {$CancellablePromise<void>}
 */
export function Resize(sessionID, cols, rows) {
    return $Call.ByID(4062454978, sessionID, cols, rows);
}

/**
 * SendInput writes user keystrokes to the shell's stdin (base64 encoded).
 * @param {string} sessionID
 * @param {string} data
 * @returns {$CancellablePromise<void>}
 */
export function SendInput(sessionID, data) {
    return $Call.ByID(1132603256, sessionID, data);
}

/**
 * @param {$models.CommandHistoryService | null} svc
 * @returns {$CancellablePromise<void>}
 */
export function SetCommandHistory(svc) {
    return $Call.ByID(2692681193, svc);
}

/**
 * @returns {$CancellablePromise<void>}
 */
export function Start() {
    return $Call.ByID(4264580798);
}

/**
 * StartLocalSession creates and starts a new local terminal session.
 * On Linux/macOS this spawns a real PTY via creack/pty for full terminal
 * emulation (cursor, colours, resize). On Windows it falls back to pipes.
 * @returns {$CancellablePromise<string>}
 */
export function StartLocalSession() {
    return $Call.ByID(3230938327);
}

/**
 * @returns {$CancellablePromise<void>}
 */
export function Stop() {
    return $Call.ByID(3792857670);
}

/**
 * @param {string} ctxID
 * @param {string} path
 * @param {string} b64
 * @returns {$CancellablePromise<void>}
 */
export function WriteFile(ctxID, path, b64) {
    return $Call.ByID(1655230639, ctxID, path, b64);
}

// Private type creation functions
const $$createType0 = $Create.Array($Create.Any);
const $$createType1 = $models.FileInfo.createFrom;
const $$createType2 = $Create.Array($$createType1);
